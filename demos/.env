# Demo .env — safe to commit, contains no real credentials.
# For real credentials, copy to a project-specific .env and add it to .gitignore.
DEMO_PROJECT=my-awesome-app
DEMO_ENVIRONMENT=production
DEMO_REGISTRY=ghcr.io/myorg
